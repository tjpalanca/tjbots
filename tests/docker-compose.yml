services:
  tjbots:
    build: 
      context: ..
      dockerfile: build/Dockerfile
    volumes:
      - type: bind
        source: ../env/test.env
        target: /run/secrets/tjbots.env
    ports:
      - 8080:8080
